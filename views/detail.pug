include mixins/all.pug
doctype html
html
  head
    meta(charset='utf-8')
    title #{item.title}
    link(rel='stylesheet', href='/stylesheets/base.css')
  
body
  header
    span
      +logo
  |
  +searchbox

  main
    h1 #{item.title}
    p #{item.description}
    img(src=item.picture)
   
